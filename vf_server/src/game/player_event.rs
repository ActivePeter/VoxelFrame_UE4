use crate::game::{Game, ClientId};
use crate::protos::common;
use crate::{conv, net_pack_convert};
use crate::net_pack_convert::PackIds;


    async fn on_player_connected() {

    }

    fn on_player_disconnected() {

    }






    fn on_player_sync_trans() {

    }

